# Hakyll Site for Tenyks.io

This site is generated with [Hakyll](http://jaspervdj.be/hakyll/) static site
generator.

# Install and build

```bash
cabal install happy alex hakyll
#or
sudo apt-get install libghc-hakyll-dev
ghc --make -threaded site.hs
./site build
```
