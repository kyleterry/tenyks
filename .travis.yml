language: go

go:
  - tip

env:
  - GO15VENDOREXPERIMENT=1

services:
  - redis-server

install:
  - echo noop

script: make test

notifications:
  irc:
    channels:
      - "irc.freenode.org#tenyks"
      - "irc.freenode.org#infoforcefeed"
