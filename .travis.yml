language: go

go:
  - tip

env:
  - GO15VENDOREXPERIMENT=1

services:
  - redis-server

install:
  - go get github.com/Masterminds/glide
  - glide install

script: go test ./...

notifications:
  irc:
    channels:
      - "irc.freenode.org#tenyks"
      - "irc.freenode.org#infoforcefeed"
